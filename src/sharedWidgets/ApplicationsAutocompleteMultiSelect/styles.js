import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  main: {
    position: 'relative',
    height: '60px',
    margin: '0 20px',
  },
  wrapper: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
  },
});

export default useStyles;
